'use client';

/**
 * OrientationGuard was removed per requirement.
 * This no-op component remains only to avoid import errors if referenced accidentally.
 * It renders nothing and contains no logic.
 */
export default function OrientationGuard() {
  return null;
}
